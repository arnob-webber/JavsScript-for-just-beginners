const student = ["ali", "hasan", "arnob"];

for (let name = 0; name < student.length; name++) {
  console.log(student[name]);
}

// for this code easy we can use for in loop

const marks = ["50", "75", "68"];

// this line mean add marks value into number
for (number of marks) {
  console.log(number);
}
